# Local Postgres for `netlify dev`, `npm run db:migrate` and `npm run seed`.
# On Netlify, Netlify DB sets NETLIFY_DATABASE_URL, which is used when DATABASE_URL is unset.
DATABASE_URL="postgresql://localhost:5432/hr_pilot_netlify"
